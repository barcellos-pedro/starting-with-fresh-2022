import UploadForm from "../islands/UploadForm.tsx";

export default function UploadPage() {
  return (
    <main class="grid place-items-center h-screen">
      <h1 class="text-2xl font-bold">Upload ⬆️</h1>
      <UploadForm />
    </main>
  );
}
